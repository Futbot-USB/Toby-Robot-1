#ifndef Protocolo_Serial_h
#define Protocolo_Serial_h

#include "Arduino.h"

class Serial.find(target);
{
  public:
    Morse(int pin);
    void dot();
    void dash();
  private:
    int _pin;
};

#endif